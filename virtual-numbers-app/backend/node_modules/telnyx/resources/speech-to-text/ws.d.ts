import * as WS from 'ws';
import { SpeechToTextEmitter } from "./internal-base.js";
import * as SpeechToTextAPI from "./speech-to-text.js";
import { Telnyx } from "../../client.js";
export declare class SpeechToTextWS extends SpeechToTextEmitter {
    url: URL;
    socket: WS.WebSocket;
    private client;
    constructor(client: Telnyx, parameters: SpeechToTextAPI.SpeechToTextStreamParams, options?: WS.ClientOptions | null | undefined);
    /**
     * Send binary audio data to the server for transcription.
     * @param data - Raw audio bytes (mp3 or wav format)
     */
    send(data: SpeechToTextAPI.SttClientEvent): void;
    /**
     * Close the WebSocket connection.
     */
    close(props?: {
        code: number;
        reason: string;
    }): void;
    /**
     * Check if the WebSocket is open and ready to send data.
     */
    get isOpen(): boolean;
    /**
     * Wait for the connection to be established.
     */
    waitForOpen(): Promise<void>;
    private authHeaders;
}
//# sourceMappingURL=ws.d.ts.map